import {
    Navbar,
    TitleHeader,
} from "../styles"

export const TopNavbar = () => {

    return(
    <Navbar>
        <TitleHeader>Welcome!</TitleHeader>
        <TitleHeader>Bazs Portfolio Site</TitleHeader>
    </Navbar>
    )
};